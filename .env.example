# Environment Configuration for Markdown Notes

# Application Settings
NODE_ENV=production
PORT=3000

# File Storage
NOTES_DIR=/app/notes
DATA_DIR=/app/data

# Optional: Custom root path for notes (defaults to NOTES_DIR)
# ROOT_PATH=/custom/path/to/notes

# Optional: Enable debug logging
# DEBUG=true

# Optional: Custom host binding (default: 0.0.0.0)
# HOST=0.0.0.0

# Optional: Maximum file size for uploads (in bytes)
# MAX_FILE_SIZE=10485760

# Optional: Allowed file extensions (comma-separated)
# ALLOWED_EXTENSIONS=.md,.txt,.markdown